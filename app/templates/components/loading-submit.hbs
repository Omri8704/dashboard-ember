{{#if loading}}
  <input disabled="disabled" type="submit" class=" disabled btn btn-primary pull-right" value="Loading...">
{{else}}
  <input disabled="{{if isntValid 'disabled'}}" type="submit" class="btn btn-primary pull-right" value="Save Campaign" >
{{/if}}
