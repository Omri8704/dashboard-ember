import { test, moduleFor } from 'ember-qunit';
moduleFor('transform:by-day-hash', 'Unit | Transform | by day hash', {});

test('it exists', function(assert) {
  var transform;
  transform = this.subject();
  assert.ok(transform);
});
