import { paginatedItemNumber } from '../../../helpers/paginated-item-number';
import { module, test } from 'qunit';

module('Unit | Helper | paginated item number');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = paginatedItemNumber([42, 3]);
  assert.ok(result);
});
