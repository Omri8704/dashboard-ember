import { humanizeGraphKey } from '../../../helpers/humanize-graph-key';
import { module, test } from 'qunit';

module('Unit | Helper | humanize graph key');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = humanizeGraphKey("42");
  assert.ok(result);
});
